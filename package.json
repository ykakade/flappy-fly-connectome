{
  "name": "flappy-fly-connectome",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server 4173 --directory dist",
    "test": "node --test tests/browser.test.mjs",
    "lint": "node --check dist/app.js && node --check dist/environment.js && node --check dist/controller.js"
  },
  "engines": {
    "node": ">=20"
  }
}
